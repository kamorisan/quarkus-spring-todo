# スクリプト依存関係マップ

## 📊 メインスクリプトと依存スクリプトの関係

### 1. build_all.sh

**呼び出される子スクリプト**: なし

**特徴**:
- 完全に自己完結型
- Maven コマンドのみを使用
- 他のスクリプトに依存しない

---

### 2. test_all_modes.sh

**呼び出される子スクリプト**:

```
test_all_modes.sh
└── smoke_test.sh (3回呼び出し: Native, JVM, Spring用)
```

**呼び出し箇所**:
```bash
# Quarkus Nativeのテスト
"$SCRIPT_DIR/smoke_test.sh" 8081

# Quarkus JVMのテスト
"$SCRIPT_DIR/smoke_test.sh" 8081

# Spring Boot JVMのテスト
"$SCRIPT_DIR/smoke_test.sh" 8082
```

---

### 3. run_benchmark.sh ⭐ 最も依存が多い

**呼び出される子スクリプト**:

```
run_benchmark.sh
├── run_quarkus_native.sh (Nativeアプリ起動)
├── wait_ready.sh (準備完了待機) - 3回
├── measure_idle.sh (メトリクス計測) - 3回
├── run_quarkus.sh (Quarkus JVMアプリ起動)
├── run_spring.sh (Spring Bootアプリ起動)
└── summary.sh (結果サマリー表示)
```

**呼び出しフロー**:

#### Quarkus Nativeのベンチマーク
```bash
1. bash "$SCRIPT_DIR/run_quarkus_native.sh"
2. bash "$SCRIPT_DIR/wait_ready.sh" "http://localhost:8081/health/ready"
3. bash "$SCRIPT_DIR/measure_idle.sh" "$QUARKUS_NATIVE_PID" 60 "$ROOT_DIR/results/quarkus-native_idle.csv"
```

#### Quarkus JVMのベンチマーク
```bash
1. bash "$SCRIPT_DIR/run_quarkus.sh"
2. bash "$SCRIPT_DIR/wait_ready.sh" "http://localhost:8081/health/ready"
3. bash "$SCRIPT_DIR/measure_idle.sh" "$QUARKUS_PID" 60 "$ROOT_DIR/results/quarkus_idle.csv"
```

#### Spring Boot JVMのベンチマーク
```bash
1. bash "$SCRIPT_DIR/run_spring.sh"
2. bash "$SCRIPT_DIR/wait_ready.sh" "http://localhost:8082/health/ready"
3. bash "$SCRIPT_DIR/measure_idle.sh" "$SPRING_PID" 60 "$ROOT_DIR/results/spring_idle.csv"
```

#### 結果表示
```bash
bash "$SCRIPT_DIR/summary.sh"
```

---

## 📋 全子スクリプト一覧

### bench/run_quarkus_native.sh

**役割**: Quarkus Nativeバイナリをバックグラウンドで起動

**呼び出し元**: run_benchmark.sh

**主な処理**:
- Nativeバイナリを実行
- バックグラウンドで起動（`&`）
- PIDを `bench/quarkus-native.pid` に保存
- ログを `logs/quarkus-native.log` に出力

**パラメータ**: なし

---

### bench/run_quarkus.sh

**役割**: Quarkus JVMモードをバックグラウンドで起動

**呼び出し元**: run_benchmark.sh

**主な処理**:
- `java -jar quarkus-todo/target/quarkus-app/quarkus-run.jar` を実行
- JVMオプション: `-Xms128m -Xmx512m`
- バックグラウンドで起動（`&`）
- PIDを `bench/quarkus.pid` に保存
- ログを `logs/quarkus.log` に出力

**パラメータ**: なし

---

### bench/run_spring.sh

**役割**: Spring Boot JVMをバックグラウンドで起動

**呼び出し元**: run_benchmark.sh

**主な処理**:
- `java -jar spring-todo/target/spring-todo-0.0.1-SNAPSHOT.jar` を実行
- JVMオプション: `-Xms128m -Xmx512m`
- バックグラウンドで起動（`&`）
- PIDを `bench/spring.pid` に保存
- ログを `logs/spring.log` に出力

**パラメータ**: なし

---

### bench/wait_ready.sh ⭐ 重要

**役割**: ヘルスチェックエンドポイントをポーリングして準備完了を待機

**呼び出し元**: run_benchmark.sh（3回）

**主な処理**:
1. 起動時刻を記録
2. ヘルスチェックエンドポイントに対してcurlリクエスト（ループ）
3. 成功するまで待機（最大タイムアウトあり）
4. 成功したら起動時間を計算して表示
5. アプリケーションログからも起動時間を抽出

**パラメータ**:
- `$1`: ヘルスチェックURL（例: `http://localhost:8081/health/ready`）

**出力**:
- 標準出力に起動時間を表示
- アプリケーションログに `APP_READY_MS=<ミリ秒>` を記録

---

### bench/measure_idle.sh ⭐ 重要

**役割**: 指定したプロセスのメモリとCPU使用率を一定期間計測

**呼び出し元**: run_benchmark.sh（3回）

**主な処理**:
1. 指定された秒数分ループ
2. 1秒ごとに `ps` コマンドでRSSとCPU%を取得
3. タイムスタンプと共にCSVファイルに保存

**パラメータ**:
- `$1`: プロセスID
- `$2`: 計測時間（秒）
- `$3`: 出力CSVファイルパス

**出力CSVフォーマット**:
```csv
timestamp,rss_kb,cpu_percent
1709017200,68432,0.0
1709017201,68456,0.1
...
```

**使用コマンド**:
```bash
ps -p $PID -o rss=,pcpu=
```

---

### bench/summary.sh ⭐ 重要

**役割**: ベンチマーク結果のCSVファイルを読み込んで統計を計算・表示

**呼び出し元**: run_benchmark.sh（最後に1回）

**主な処理**:
1. 各CSVファイル（`quarkus-native_idle.csv`, `quarkus_idle.csv`, `spring_idle.csv`）を読み込み
2. RSSとCPUの平均値を計算
3. 起動時間をログファイルから抽出（`APP_READY_MS`）
4. 3-Way比較のサマリーを整形して表示
5. 倍率とメモリ削減率を計算

**パラメータ**: なし

**使用ツール**:
- `awk`: CSV解析、平均値計算
- `bc`: 高精度計算
- `grep`, `sed`: ログ解析

**出力例**:
```
=========================================
  Quarkus vs Spring Boot Benchmark Summary
  (3-Way Comparison)
=========================================

Quarkus Native Startup Time: 15ms
Quarkus JVM Startup Time: 51ms
Spring Boot JVM Startup Time: 712ms

-----------------------------------------
Memory Usage (Idle)
-----------------------------------------
Quarkus Native: 48 MB
Quarkus JVM:    224 MB
Spring JVM:     316 MB

-----------------------------------------
Summary Comparison:
-----------------------------------------
Startup Time:
  Quarkus Native: 15ms (1.0x baseline)
  Quarkus JVM:    51ms (3.4x slower than Native)
  Spring JVM:     712ms (47.5x slower than Native)

Memory Savings:
  Native saves 78.6% vs Quarkus JVM
  Native saves 84.8% vs Spring JVM
=========================================
```

---

### bench/smoke_test.sh

**役割**: 基本的なCRUD操作を短時間でテスト

**呼び出し元**: test_all_modes.sh（3回）、bench/test_api.sh

**主な処理**:
1. ヘルスチェック
2. Todo作成（POST）
3. 一覧取得（GET）
4. ID指定取得（GET）
5. 更新（PATCH）
6. 削除（DELETE）

**パラメータ**:
- `$1`: ポート番号（デフォルト: 8081）

**実行時間**: 約2秒

**終了コード**:
- `0`: 全テスト成功
- `1`: テスト失敗

---

### bench/test_api.sh

**役割**: 全CRUD操作とバリデーションを詳細にテスト

**呼び出し元**: （ユーザーが直接実行）

**主な処理**:
1. サーバーのヘルスチェック
2. CREATE - 複数のTodo作成
3. READ - 全取得と個別取得
4. UPDATE - PUT（全更新）とPATCH（部分更新）
5. DELETE - 削除と削除確認
6. VALIDATION - 不正データのテスト

**パラメータ**:
- `$1`: ポート番号（デフォルト: 8081）

**実行時間**: 約5秒

**依存関係**: なし（他のスクリプトを呼び出さない）

---

### bench/load_test.sh

**役割**: 負荷テスト（現在は使用されていない）

**呼び出し元**: なし

**状態**: 未使用（将来の拡張用？）

---

## 📊 依存関係の可視化

### 依存関係ツリー

```
メインスクリプト
│
├── build_all.sh (独立)
│
├── test_all_modes.sh
│   └── smoke_test.sh × 3回
│
└── run_benchmark.sh
    ├── run_quarkus_native.sh
    ├── wait_ready.sh × 3回
    ├── measure_idle.sh × 3回
    ├── run_quarkus.sh
    ├── run_spring.sh
    └── summary.sh
```

### 実行頻度

| スクリプト | 呼び出し元 | 実行回数 |
|-----------|----------|---------|
| **smoke_test.sh** | test_all_modes.sh | 3回（各モード1回） |
| **run_quarkus_native.sh** | run_benchmark.sh | 1回 |
| **run_quarkus.sh** | run_benchmark.sh | 1回 |
| **run_spring.sh** | run_benchmark.sh | 1回 |
| **wait_ready.sh** | run_benchmark.sh | 3回（各モード1回） |
| **measure_idle.sh** | run_benchmark.sh | 3回（各モード1回） |
| **summary.sh** | run_benchmark.sh | 1回（最後） |

---

## 🔍 各スクリプトの詳細確認方法

子スクリプトの内容を確認したい場合：

```bash
# 起動スクリプト
cat bench/run_quarkus_native.sh
cat bench/run_quarkus.sh
cat bench/run_spring.sh

# 計測スクリプト
cat bench/wait_ready.sh
cat bench/measure_idle.sh

# 結果表示スクリプト
cat bench/summary.sh

# テストスクリプト
cat bench/smoke_test.sh
cat bench/test_api.sh
```

---

## 📝 スクリプト作成の推奨順序

もしこれらのスクリプトを理解したい、または修正したい場合は、以下の順序で読むことをおすすめします：

### レベル1: 基本スクリプト（依存なし）
1. `run_quarkus_native.sh` - 起動のみ
2. `run_quarkus.sh` - 起動のみ
3. `run_spring.sh` - 起動のみ

### レベル2: 計測スクリプト（基本的な処理）
4. `wait_ready.sh` - ポーリング処理
5. `measure_idle.sh` - メトリクス取得

### レベル3: 集計スクリプト（複雑な処理）
6. `summary.sh` - CSV解析、統計計算

### レベル4: テストスクリプト
7. `smoke_test.sh` - 基本テスト
8. `test_api.sh` - 詳細テスト

### レベル5: 統合スクリプト（全てを組み合わせ）
9. `test_all_modes.sh` - テスト自動化
10. `run_benchmark.sh` - ベンチマーク自動化
11. `build_all.sh` - ビルド自動化

---

## 🎯 まとめ

### 主要な子スクリプト（必須）

**ベンチマークに必要**:
- `run_quarkus_native.sh`
- `run_quarkus.sh`
- `run_spring.sh`
- `wait_ready.sh` ⭐
- `measure_idle.sh` ⭐
- `summary.sh` ⭐

**テストに必要**:
- `smoke_test.sh` ⭐

### オプショナル

- `test_api.sh` - 詳細テスト用（手動実行）
- `load_test.sh` - 現在未使用

### 最も重要な3つのスクリプト

1. **wait_ready.sh** - 起動時間計測の核心
2. **measure_idle.sh** - パフォーマンス計測の核心
3. **summary.sh** - 結果分析の核心

これらがなければベンチマークは機能しません。
